<?php

// Intentionally empty. This service is a pure JSON API — see Rule Zero in
// docs/design/01-domain-design.md. No web middleware group, no session
// driver, no Blade views, no redirects. All routes live in routes/api.php.
